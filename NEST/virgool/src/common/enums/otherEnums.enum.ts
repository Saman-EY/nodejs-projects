export enum CookieKeys {
  Otp = "otp",
  EmailOtp = "email-otp",
  PhoneOtp = "phone-otp",
}

export enum Gender {
  man = "مرد",
  woman = "زن",
  others = "سایر",
}

export enum BlogStatus {
  Published = "published",
  Draft = "draft",
  Rejected = "rejected",
}

export enum SmsTemplate {
  Verify = "verify",
}

export enum Roles {
  Admin = "admin",
  User = "user",
}

export enum UserStatus {
  Ban = "ban",
  Report = "report",
}
